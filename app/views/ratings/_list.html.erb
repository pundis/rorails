<table class="table table-hover">
  <thead>
  <tr>
    <th></th>
    <th>Average</th>
  </tr>
  </thead>

  <tbody>
  <% source.each do |item| %>
      <tr>
        <td><%= link_to item.name, item %></td>
        <td><%= item.average_rating %></td>
      </tr>
  <% end %>
  </tbody>
</table>
